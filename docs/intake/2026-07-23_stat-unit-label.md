# 要件定義 / インテーク（1枚）— 統計欄「15」の単位表記追加

### 機能名
トップページ統計欄（stat）の「15」に単位「体」を追加

### 目的・価値（逆算した真の狙い）
「7本」「6本」「120→9秒」は単位付きなのに「15」だけ単位が無く不揃いに見える。見た目の統一感を整える軽微なテキスト修正。

### 対象ユーザー
Kaneta本人（サイト管理者）

### 機能要件（Must）
- Must: `index.html` 内 `<div class="stat">` の「15」（AIエージェント組織）の `<span class="u">` に「体」を追加し「15体」と表示する

### スコープ / 非スコープ
- やること: index.html 330行目付近の該当spanのテキスト変更のみ
- やらないこと: 他の統計項目・レイアウト・スタイルの変更

### 既存への影響（壊れうる箇所）
CSS `.u` クラスの見た目（フォントサイズ等）に既存の想定内。他の3項目（本/本/→9秒）と同一クラスなので表示崩れなし。

### 段取り
- 担当（実装）: 本セッション（Claude Code）
- 人間GOが要る箇所: なし（git push含め本人希望に沿って実施。デプロイ=公開Pagesへの反映を伴うため実施前に確認）

### 承認
Kaneta本人が本会話内でスクリーンショット付きで直接指示（2026-07-23）。

承認: 済み
